# 为什么改动对话前缀会影响缓存

[本章实验目录](../README.md) · [相关正文](../../book/chapter2.md) · [技术参考](REFERENCE.md)

两轮请求大部分内容相同，第二轮为什么有时仍然很慢？本实验从消息组织方式入手，观察缓存能否复用，以及看似无关的上下文改动怎样影响成本。

## 理解实验

因果模型在生成后续词元时可以复用前缀的计算。若每轮都改写前面的系统消息、重排工具定义或移动历史窗口，原来相同的前缀就可能变短。缓存复用关注的是实际输入序列，而不是两段文字在人看来是否意思相同。

## 动手之前

本节先使用本地示例或已有数据，不需要模型 API Key。安装依赖仍可能需要联网。 通用环境说明见[实验学习指南](../../docs/EXPERIMENTS.md)。

## 一步步观察

先用下方命令读取目录中已有的结果，不发模型请求。对照缓存命中和输入量，再阅读 `agent.py` 中不同模式怎样构造消息。需要重新测量时，按技术参考配置模型，在同一任务上分别运行正确模式和一种改动模式。

以下命令从本实验目录运行；请先完成上面的环境准备。

```bash
python main.py --report
```

## 怎样解释结果

缓存命中量、延迟和价格是相关但不同的量。延迟还受网络与服务负载影响；费用计算取决于提供商如何计价。不要用一次更快的响应证明缓存有效，也不要把估算价格当作账单。

## 继续思考

如果只在消息末尾追加状态信息，与每轮改写系统提示词中的状态，缓存表现会有什么不同？

## 阅读代码与技术参考

沿下面的顺序阅读代码，可以把前面的概念与实现对应起来：[main.py](https://github.com/bojieli/ai-agent-book/blob/main/chapter2/kv-cache/main.py) → [agent.py](https://github.com/bojieli/ai-agent-book/blob/main/chapter2/kv-cache/agent.py)。

原有说明保存在[技术参考](REFERENCE.md)中。需要查阅详细配置、英文材料或历史记录时，请从这里继续，并核对记录所使用的数据、模型与环境条件。
